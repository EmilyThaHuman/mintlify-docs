---
title: "Quickstart"
description: "Get up and running with ReedAI Assistant in minutes"
---

# Quickstart Guide

This guide will help you get ReedAI Assistant up and running in your development environment.

## Prerequisites

Before you begin, make sure you have the following installed:

- Node.js 16.x or higher
- npm 7+ or yarn 1.22+
- Git

<Note>
  ReedAI Assistant requires environment variables for API access. Make sure to
  set these up before using the application.
</Note>

## Installation

<Steps>
  <Step title="Clone the Repository">
    ```bash
    git clone https://github.com/yourusername/reedai-assistant.git
    cd reedai-assistant
    ```
  </Step>

<Step title="Install Dependencies">
  ```bash npm install # or yarn install ```
</Step>

  <Step title="Configure Environment Variables">
    Create a `.env` file in the root directory by copying the example:

    ```bash
    cp .env.example .env
    ```

    Then edit the `.env` file to add your API keys:

    ```
    ANTHROPIC_API_KEY=your_anthropic_api_key
    OPENAI_API_KEY=your_openai_api_key
    # Add other required environment variables
    ```

  </Step>

  <Step title="Start the Development Server">
    ```bash
    npm run dev
    # or
    yarn dev
    ```
  </Step>
</Steps>

## Accessing the Application

Once the development server is running, you can access the application at:

```
http://localhost:5173
```

## Next Steps

Now that you have ReedAI Assistant up and running, you can:

1. **Configure Your Agents**: Customize the AI agents to match your development needs.
2. **Create Your First Thread**: Start a new conversation with one of the specialized agents.
3. **Explore the Features**: Try out code generation, documentation creation, and more.

<CardGroup>
  <Card title="AI Agents" icon="robot" href="/concepts/ai-agents">
    Learn about the specialized AI agents
  </Card>
  <Card
    title="Architecture"
    icon="diagram-project"
    href="/concepts/architecture"
  >
    Learn how ReedAI Assistant works
  </Card>
</CardGroup>
